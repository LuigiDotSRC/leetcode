from typing import List

# sequences intuition
# using hash sets for O(1) lookup

class Solution:
    def longestConsequtive(self, nums: List[int]) -> int:
        numSet = set(nums)
        longest = 0

        for num in numSet:
            if (num - 1) not in numSet:
                length = 1
                while (num + length) in numSet:
                    length += 1
                longest = max(length, longest)
        return longest
